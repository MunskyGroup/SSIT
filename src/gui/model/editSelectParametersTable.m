function [] = editSelectParametersTable(app)
% This function takes the edited Table of Select Parameters and creates a
% list of parameters with their corresponding values to include
% in the sensitivity analysis. 

cla(app.SensProbAxes); 
cla(app.SensDerivativeAxes); 

end